# Tesseract configuration file for GPU deployment

name: "wdm"
version: "0+unknown"
description: ""

build_config:
  base_image: "nvidia/cuda:12.9.1-devel-ubuntu24.04"
  target_platform: "native"
  custom_build_steps:
    - "RUN apt-get update && apt-get install -y python3-pip python3-setuptools python3-distutils"
